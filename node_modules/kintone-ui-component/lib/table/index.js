var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { html } from "lit";
import { property, query } from "lit/decorators.js";
import { ERROR_MESSAGE } from "../base/constant";
import { unsafeHTMLConverter, visiblePropConverter } from "../base/converter";
import { createStyleOnHeader, dispatchCustomEvent, KucBase, } from "../base/kuc-base";
import { isHTMLElement, validateArrayType, validateFieldRequiredInColumnTable, validateFieldUniqueInColumnTable, validateProps, } from "../base/validator";
import { TABLE_CSS } from "./style";
const cellClassName = "kuc-table-1-23-1__table__body__row__cell-data";
const rowClassName = "kuc-table-1-23-1__table__body__row";
const cellActionsClassName = "kuc-table-1-23-1__table__body__row__action";
const btnAddRowClassName = "kuc-table-1-23-1__table__body__row__action-add";
const btnRemoveRowClassName = "kuc-table-1-23-1__table__body__row__action-remove";
const customWidthVariables = (index) => `var(--kuc-table-header-${index}-width, var(--kuc-table-header-width, auto))`;
const dAdd = "M8 16C12.4183 16 16 12.4183 16 8C16 3.58172 12.4183 0 8 0C3.58172 0 0 3.58172 0 8C0 12.4183 3.58172 16 8 16ZM12.0355 8.49997V7.49997H8.50008V3.96454H7.50008V7.49997H3.96443V8.49997H7.50008V12.0356H8.50008V8.49997H12.0355Z";
const fillAdd = "#3498db";
const dRemove = "M16 8C16 12.4183 12.4183 16 8 16C3.58172 16 0 12.4183 0 8C0 3.58172 3.58172 0 8 0C12.4183 0 16 3.58172 16 8ZM12.0355 7.49997V8.49997L3.96443 8.49997V7.49997H12.0355Z";
const fillRemove = "#b5b5b5";
let exportTable;
(() => {
    exportTable = window.customElements.get("kuc-table-1-23-1");
    if (exportTable) {
        return;
    }
    class KucTable extends KucBase {
        constructor(props) {
            super();
            this.actionButtonPosition = "right";
            this.className = "";
            this.id = "";
            this.label = "";
            this.columns = [];
            this.data = [];
            this.actionButton = true;
            this.headerVisible = true;
            this.visible = true;
            this._actionButton = {
                add: true,
                remove: true,
            };
            this._actionButtonPosition = "right";
            if (!props)
                return;
            const validProps = validateProps(props);
            Object.assign(this, validProps);
        }
        shouldUpdate(_changedProperties) {
            if (_changedProperties.has("data") || _changedProperties.has("columns")) {
                const errorMessage = this._getErrorValidateColumnsAndData();
                if (errorMessage) {
                    this.throwErrorAfterUpdateComplete(errorMessage);
                    return false;
                }
            }
            return true;
        }
        willUpdate(_changedProperties) {
            if (_changedProperties.has("actionButtonPosition")) {
                this._actionButtonPosition =
                    this.actionButtonPosition === "left" ? "left" : "right";
            }
            if (_changedProperties.has("actionButton")) {
                this._actionButton = this._getActionButtonSettings();
            }
            if (!this._tBody)
                return;
            this._tBody.innerHTML = "";
        }
        render() {
            return !this.columns || this.columns.length < 1
                ? html `<table class="kuc-table-1-23-1__table">
            <caption
              class="kuc-table-1-23-1__table__label kuc-table-1-23-1__table__label--no-column"
              ?hidden="${!this.label}"
            >
              ${this.label}
            </caption>
          </table>`
                : html `
            <table class="kuc-table-1-23-1__table">
              <caption class="kuc-table-1-23-1__table__label" ?hidden="${!this.label}">
                ${this.label}
              </caption>
              <thead
                class="kuc-table-1-23-1__table__header"
                ?hidden="${!this.headerVisible}"
              >
                ${this._getTableHeaderTemplate()}
              </thead>
              <tbody
                class="kuc-table-1-23-1__table__body${!this.headerVisible
                    ? " kuc-table-1-23-1__table__body--no-header"
                    : ""}"
              ></tbody>
            </table>
          `;
        }
        updated(_changedProperties) {
            if (this.columns.length === 0)
                return;
            for (let i = 0; i < this.data.length; i++) {
                this._addRowToTable(i, this.data[i]);
            }
        }
        _getTableHeaderTemplate() {
            return html `
        <tr>
          ${this._actionButtonPosition === "left"
                ? this._getActionButtonHeaderTemplate()
                : ""}
          ${this.columns.map((column, index) => this._getColumnHeaderTemplate(column, index))}
          ${this._actionButtonPosition === "right"
                ? this._getActionButtonHeaderTemplate()
                : ""}
        </tr>
      `;
        }
        _getActionButtonHeaderTemplate() {
            if (!this.data ||
                this.data.length < 1 ||
                (!this._actionButton.add && !this._actionButton.remove))
                return html ``;
            return html `
        <th
          class="kuc-table-1-23-1__table__header__cell kuc-table-1-23-1__table__header__cell__action${this
                ._actionButtonPosition === "left"
                ? "--left"
                : "--right"}"
        ></th>
      `;
        }
        _getColumnHeaderTemplate(column, index) {
            const customWidth = customWidthVariables(index);
            return html `
        <th
          class="kuc-table-1-23-1__table__header__cell"
          ?hidden="${column.visible === false}"
          style="width: ${customWidth}; min-width: ${customWidth}; max-width: ${customWidth}"
        >
          <div class="kuc-table-1-23-1__table__header__cell-title">
            ${column.title && isHTMLElement(column.title)
                ? unsafeHTMLConverter(column.title)
                : column.title}<!--
        --><span
              class="kuc-base-label-1-23-1__required-icon"
              ?hidden="${!column.requiredIcon}"
              >*</span
            >
          </div>
        </th>
      `;
        }
        _getActionsCellWhenRemoveRow(currentRowIndex) {
            let actionsCell = null;
            let i = currentRowIndex;
            while (this.data.length > 1) {
                const currentRow = this._table.rows[i];
                if (!currentRow) {
                    i--;
                }
                else {
                    actionsCell =
                        currentRow.cells[this._actionButtonPosition === "left" ? 0 : this.columns.length];
                    break;
                }
            }
            return actionsCell;
        }
        _getDefaultDataRow(data) {
            const defaultRowData = {};
            for (const key in data) {
                if (Object.prototype.hasOwnProperty.call(data, key)) {
                    if (Array.isArray(data[key])) {
                        defaultRowData[key] = [];
                        continue;
                    }
                    if (typeof data[key] === "object" && data[key] !== null) {
                        defaultRowData[key] = {};
                        continue;
                    }
                    defaultRowData[key] = "";
                }
            }
            return defaultRowData;
        }
        _addRowToTable(currentRowIndex, defaultRow) {
            var _a;
            const newRow = this._tBody.insertRow(currentRowIndex);
            newRow.classList.add(rowClassName);
            if ((this._actionButton.add || this._actionButton.remove) &&
                this._actionButtonPosition === "left") {
                this._addActionsCellToNewRow(newRow);
            }
            for (let i = 0; i < this.columns.length; i++) {
                const customWidth = customWidthVariables(i);
                const newCell = newRow.insertCell(i +
                    ((this._actionButton.add || this._actionButton.remove) &&
                        this._actionButtonPosition === "left"
                        ? 1
                        : 0));
                const column = this.columns[i];
                newCell.classList.add(cellClassName);
                newCell.style.width = customWidth;
                newCell.style.maxWidth = customWidth;
                newCell.style.minWidth = customWidth;
                newCell.addEventListener("change", (event) => {
                    this._handleChangeCell(event, column.field);
                });
                newCell.hidden = !((_a = column.visible) !== null && _a !== void 0 ? _a : true);
                const cellTemplate = column.render
                    ? column.render(defaultRow[column.field], defaultRow, currentRowIndex)
                    : defaultRow[column.field];
                if (cellTemplate && cellTemplate.nodeType) {
                    newCell.appendChild(cellTemplate);
                }
                else {
                    newCell.innerText = cellTemplate || "";
                }
            }
            if ((!this._actionButton.add && !this._actionButton.remove) ||
                this._actionButtonPosition === "left")
                return;
            this._addActionsCellToNewRow(newRow);
        }
        _handleChangeCell(event, field) {
            event.stopPropagation();
            const oldData = this._deepCloneObject(this.data);
            const currentRow = event.currentTarget
                .parentElement;
            const dataIndex = currentRow.rowIndex - 1;
            const dataRow = this.data[dataIndex];
            if (field in dataRow) {
                let _newValue = event.target.value;
                if ("detail" in event) {
                    _newValue = event.detail.value;
                }
                dataRow[field] = _newValue;
            }
            const data = {
                type: "change-cell",
                rowIndex: dataIndex,
                data: this._deepCloneObject(this.data),
                oldData: oldData,
                field: field,
            };
            this._dispatchChangeEvent(data);
        }
        _handleAddRow(currentRowIndex) {
            const oldData = this._deepCloneObject(this.data);
            const defaultDataRow = this._getDefaultDataRow(this.data[0]);
            this._addRowToTable(currentRowIndex, defaultDataRow);
            this.data.splice(currentRowIndex, 0, defaultDataRow);
            const data = {
                type: "add-row",
                rowIndex: currentRowIndex,
                data: this._deepCloneObject(this.data),
                oldData: oldData,
            };
            this._dispatchChangeEvent(data);
            this._toggleRemoveRowButton();
        }
        _handleRemoveRow(currentRowIndex) {
            if (this.data.length === 1)
                return;
            const dataIndexRemoved = currentRowIndex - 1;
            const oldData = this._deepCloneObject(this.data);
            this._table.deleteRow(currentRowIndex);
            this.data.splice(dataIndexRemoved, 1);
            const data = {
                type: "remove-row",
                rowIndex: dataIndexRemoved,
                data: this._deepCloneObject(this.data),
                oldData: oldData,
            };
            this._dispatchChangeEvent(data);
            this._toggleRemoveRowButton();
            this._focusActionsButtonWhenRemoveRow(currentRowIndex);
        }
        _focusActionsButtonWhenRemoveRow(currentRowIndex) {
            const actionsCell = this._getActionsCellWhenRemoveRow(currentRowIndex);
            if (actionsCell) {
                this._focusRemoveRowButton(actionsCell);
                return;
            }
            this._focusFirstAddRowButton();
        }
        _focusRemoveRowButton(actionsCell) {
            const removeRowButton = actionsCell.querySelector(`.${btnRemoveRowClassName}`);
            removeRowButton.focus();
        }
        _focusFirstAddRowButton() {
            const firstActionsCell = this._table.rows[1].cells[this._actionButtonPosition === "left" ? 0 : this.columns.length];
            const addRowButton = firstActionsCell.querySelector(`.${btnAddRowClassName}`);
            addRowButton === null || addRowButton === void 0 ? void 0 : addRowButton.focus();
        }
        _toggleRemoveRowButton() {
            const firstActionsCell = this._actionButtonPosition === "left"
                ? this._tBody.rows[0].firstChild
                : this._tBody.rows[0].lastChild;
            const firstRemoveRow = firstActionsCell.lastChild;
            if (this.data.length === 1) {
                firstRemoveRow.style.display = "none";
                if (!this._actionButton.add) {
                    firstActionsCell.style.display = "none";
                    this._hideActionHeaderCell();
                }
                return;
            }
            if (this.data.length === 2) {
                const secondActionsCell = this._actionButtonPosition === "left"
                    ? this._tBody.rows[1].firstChild
                    : this._tBody.rows[1].lastChild;
                const secondRemoveRow = secondActionsCell.lastChild;
                firstRemoveRow.style.display = secondRemoveRow.style.display =
                    "inline-block";
                firstActionsCell.style.removeProperty("display");
            }
        }
        _getSvgDOM(fillPath, dPath) {
            const iconSvg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
            iconSvg.setAttribute("fill", "none");
            iconSvg.setAttribute("width", "18");
            iconSvg.setAttribute("height", "18");
            iconSvg.setAttribute("viewBox", "0 0 16 16");
            iconSvg.setAttribute("aria-hidden", "true");
            const iconPath = document.createElementNS("http://www.w3.org/2000/svg", "path");
            iconPath.setAttribute("d", dPath);
            iconPath.setAttribute("fill-rule", "evenodd");
            iconPath.setAttribute("clip-rule", "evenodd");
            iconPath.setAttribute("fill", fillPath);
            iconSvg.appendChild(iconPath);
            return iconSvg;
        }
        _addActionsCellToNewRow(newRow) {
            if (!this._actionButton.add && !this._actionButton.remove) {
                return;
            }
            const newCell = newRow.insertCell(this._actionButtonPosition === "left" ? 0 : this.columns.length);
            newCell.classList.add(cellActionsClassName);
            if (this._actionButtonPosition === "left") {
                newCell.classList.add("kuc-table-1-23-1__table__body__row__action--left");
            }
            else {
                newCell.classList.add("kuc-table-1-23-1__table__body__row__action--right");
            }
            if (this._actionButton.add) {
                const btnAddDOM = this._getActionButtonDOM("add", newRow);
                newCell.appendChild(btnAddDOM);
            }
            if (this._actionButton.remove) {
                const btnRemoveDOM = this._getActionButtonDOM("remove", newRow);
                newCell.appendChild(btnRemoveDOM);
                if (this.data.length === 1) {
                    btnRemoveDOM.style.display = "none";
                }
            }
            if (!this._actionButton.add && this.data.length === 1) {
                this._hideActionHeaderCell();
                newCell.style.display = "none";
            }
            else {
                this._showActionHeaderCell();
                newCell.style.removeProperty("display");
            }
        }
        _getActionButtonDOM(type, newRow) {
            const titleDeleteRow = "Delete this row";
            const titleAddRow = "Add row";
            let className = btnRemoveRowClassName;
            let title = titleDeleteRow;
            const isAdd = type === "add";
            if (isAdd) {
                className = btnAddRowClassName;
                title = titleAddRow;
            }
            const fillPath = isAdd ? fillAdd : fillRemove;
            const dPath = isAdd ? dAdd : dRemove;
            const svgEl = this._getSvgDOM(fillPath, dPath);
            const buttonAction = document.createElement("button");
            buttonAction.classList.add(className);
            buttonAction.setAttribute("title", title);
            buttonAction.type = "button";
            buttonAction.appendChild(svgEl);
            buttonAction.addEventListener("click", () => {
                const errorMessage = this._getErrorValidateColumnsAndData();
                if (errorMessage) {
                    this.throwErrorAfterUpdateComplete(errorMessage);
                    return;
                }
                if (isAdd) {
                    this._handleAddRow(newRow.rowIndex);
                    return;
                }
                this._handleRemoveRow(newRow.rowIndex);
            });
            return buttonAction;
        }
        _getActionButtonSettings() {
            const actionButton = {
                add: true,
                remove: true,
            };
            if (!this.actionButton) {
                actionButton.add = actionButton.remove = false;
                return actionButton;
            }
            if (typeof this.actionButton === "object") {
                actionButton.add = Object.prototype.hasOwnProperty.call(this.actionButton, "add")
                    ? !!this.actionButton.add
                    : true;
                actionButton.remove = Object.prototype.hasOwnProperty.call(this.actionButton, "remove")
                    ? !!this.actionButton.remove
                    : true;
            }
            return actionButton;
        }
        _getErrorValidateColumnsAndData() {
            const errorColumns = this._getErrorMessageWhenValidateColumns();
            if (errorColumns)
                return errorColumns;
            if (!validateArrayType(this.data))
                return ERROR_MESSAGE.DATA.IS_NOT_ARRAY;
            return "";
        }
        _getErrorMessageWhenValidateColumns() {
            if (!validateArrayType(this.columns)) {
                return ERROR_MESSAGE.COLUMNS.IS_NOT_ARRAY;
            }
            if (!validateFieldRequiredInColumnTable(this.columns)) {
                return ERROR_MESSAGE.COLUMNS.FIELD_REQUIRED;
            }
            if (validateFieldUniqueInColumnTable(this.columns)) {
                return ERROR_MESSAGE.COLUMNS.FIELD_UNIQUE;
            }
            return "";
        }
        _deepCloneObject(obj) {
            return JSON.parse(JSON.stringify(obj));
        }
        _dispatchChangeEvent(_detail) {
            const detail = _detail;
            dispatchCustomEvent(this, "change", detail);
        }
        _hideActionHeaderCell() {
            this._actionHeaderCellRight &&
                (this._actionHeaderCellRight.hidden = true);
            this._actionHeaderCellLeft && (this._actionHeaderCellLeft.hidden = true);
        }
        _showActionHeaderCell() {
            this._actionHeaderCellRight &&
                (this._actionHeaderCellRight.hidden = false);
            this._actionHeaderCellLeft && (this._actionHeaderCellLeft.hidden = false);
        }
    }
    __decorate([
        property({ type: String })
    ], KucTable.prototype, "actionButtonPosition", void 0);
    __decorate([
        property({ type: String, reflect: true, attribute: "class" })
    ], KucTable.prototype, "className", void 0);
    __decorate([
        property({ type: String, reflect: true, attribute: "id" })
    ], KucTable.prototype, "id", void 0);
    __decorate([
        property({ type: String })
    ], KucTable.prototype, "label", void 0);
    __decorate([
        property({ type: Array })
    ], KucTable.prototype, "columns", void 0);
    __decorate([
        property({ type: Array })
    ], KucTable.prototype, "data", void 0);
    __decorate([
        property()
    ], KucTable.prototype, "actionButton", void 0);
    __decorate([
        property({ type: Boolean })
    ], KucTable.prototype, "headerVisible", void 0);
    __decorate([
        property({
            type: Boolean,
            attribute: "hidden",
            reflect: true,
            converter: visiblePropConverter,
        })
    ], KucTable.prototype, "visible", void 0);
    __decorate([
        query(".kuc-table-1-23-1__table")
    ], KucTable.prototype, "_table", void 0);
    __decorate([
        query(".kuc-table-1-23-1__table__body")
    ], KucTable.prototype, "_tBody", void 0);
    __decorate([
        query(".kuc-table-1-23-1__table__header__cell__action--right")
    ], KucTable.prototype, "_actionHeaderCellRight", void 0);
    __decorate([
        query(".kuc-table-1-23-1__table__header__cell__action--left")
    ], KucTable.prototype, "_actionHeaderCellLeft", void 0);
    window.customElements.define("kuc-table-1-23-1", KucTable);
    createStyleOnHeader(TABLE_CSS);
    exportTable = KucTable;
})();
const Table = exportTable;
export { Table };
