"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = __importDefault(require("fs"));
const request = require("request-promise");
const messages_1 = require("./messages");
class KintoneRequest {
    constructor(auth, kintoneUrl, path, method, body, options) {
        this.auth = auth;
        this.kintoneUrl = kintoneUrl;
        this.path = path;
        this.method = method;
        this.body = body;
        this.options = options;
        this.requestOptions = {
            method: this.method,
            url: this.kintoneUrl + this.path,
            headers: {
                'X-Cybozu-Authorization': this.auth,
                'Content-Type': 'application/json'
            },
            body: this.body
        };
        if (this.options.guestSpaceId) {
            this.requestOptions.url =
                this.kintoneUrl +
                    `/k/guest/${this.options.guestSpaceId}` +
                    this.path.slice(2);
        }
        if (this.options.proxy) {
            this.requestOptions.proxy = this.options.proxy;
            this.requestOptions.tunnnel = true;
        }
    }
    get reqOptions() {
        return this.requestOptions;
    }
    set proxy(value) {
        this.requestOptions.proxy = value;
    }
    set tunnnel(value) {
        this.requestOptions.tunnel = value;
    }
    set contentType(value) {
        this.requestOptions.headers['Content-Type'] = value;
    }
    set contentLength(value) {
        this.requestOptions.headers['Content-Length'] = value;
    }
    set formData(value) {
        this.requestOptions.formData = value;
    }
    set qs(value) {
        this.requestOptions.qs = JSON.stringify(value);
    }
    send() {
        return __awaiter(this, void 0, void 0, function* () {
            return yield request(this.requestOptions).then(resp => JSON.parse(resp));
        });
    }
}
exports.default = KintoneRequest;
class UploadFile extends KintoneRequest {
    constructor(auth, kintoneUrl, filePath, contentType, options) {
        super(auth, kintoneUrl, '/k/v1/file.json', 'POST', null, options);
        this.auth = auth;
        this.kintoneUrl = kintoneUrl;
        this.filePath = filePath;
        this.contentType = contentType;
        this.options = options;
        this.contentType = 'multipart/form-data';
        this.formData = {
            file: {
                value: fs_1.default.createReadStream(filePath, 'utf8'),
                options: { contentType }
            }
        };
    }
}
exports.UploadFile = UploadFile;
class UpdateCustomizeSetting extends KintoneRequest {
    constructor(auth, kintoneUrl, body, options) {
        super(auth, kintoneUrl, '/k/v1/preview/app/customize.json', 'PUT', JSON.stringify(body), options);
        this.auth = auth;
        this.kintoneUrl = kintoneUrl;
        this.body = body;
        this.options = options;
    }
}
exports.UpdateCustomizeSetting = UpdateCustomizeSetting;
class DeploySetting extends KintoneRequest {
    constructor(auth, kintoneUrl, appId, options) {
        super(auth, kintoneUrl, '/k/v1/preview/app/deploy.json', 'POST', JSON.stringify({ apps: [{ app: appId }] }), options);
        this.auth = auth;
        this.kintoneUrl = kintoneUrl;
        this.appId = appId;
        this.options = options;
    }
}
exports.DeploySetting = DeploySetting;
class DeployStatus extends KintoneRequest {
    constructor(auth, kintoneUrl, appId, options) {
        super(auth, kintoneUrl, '/k/v1/preview/app/deploy.json', 'GET', JSON.stringify({ apps: [appId] }), options);
        this.auth = auth;
        this.kintoneUrl = kintoneUrl;
        this.appId = appId;
        this.options = options;
    }
    check() {
        return __awaiter(this, void 0, void 0, function* () {
            const m = messages_1.getBoundMessage(this.options.lang);
            let deployed = false;
            while (!deployed) {
                yield new Promise(r => setTimeout(r, 1000));
                yield this.send().then(resp => {
                    const successedApps = resp.apps;
                    const successedAppsLength = successedApps.filter(r => {
                        return r.status === 'SUCCESS';
                    }).length;
                    deployed = successedAppsLength === resp.apps.length;
                });
                console.log(m('M_Deploying'));
            }
            deployed = true;
        });
    }
}
exports.DeployStatus = DeployStatus;
