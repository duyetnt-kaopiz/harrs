"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const request_1 = require("./request");
const messages_1 = require("./messages");
function getXCybozuAuthorization(username, password) {
    const buffer = new Buffer(username + ':' + password);
    return buffer.toString('base64');
}
exports.getXCybozuAuthorization = getXCybozuAuthorization;
;
function getCustomizeUploadParams(auth, kintoneUrl, files, contentType, options) {
    return __awaiter(this, void 0, void 0, function* () {
        const m = messages_1.getBoundMessage(options.lang);
        const exp = /https?:\/\/(www\.)?[-a-zA-Z0-9@:%._+~#=]{2,256}\.[a-z]{2,6}\b([-a-zA-Z0-9@:%_+.~#?&//=]*)/gi;
        const regexp = new RegExp(exp);
        return yield Promise.all(files.map((fileOrUrl) => __awaiter(this, void 0, void 0, function* () {
            const isUrl = Boolean(fileOrUrl.match(regexp));
            if (isUrl) {
                console.log(`${fileOrUrl} ` + m('M_Uploaded'));
                return {
                    type: 'URL',
                    url: fileOrUrl
                };
            }
            else {
                const { fileKey } = yield new request_1.UploadFile(auth, kintoneUrl, fileOrUrl, contentType, options).send();
                console.log(`${fileOrUrl} ` + m('M_Uploaded'));
                return {
                    type: 'FILE',
                    file: {
                        fileKey: fileKey
                    }
                };
            }
        })));
    });
}
exports.getCustomizeUploadParams = getCustomizeUploadParams;
